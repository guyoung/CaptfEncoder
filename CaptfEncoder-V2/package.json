{
  "name": "captfencoder",
  "version": "2.0.0",
  "description": "CaptfEncoder",
  "main": "index.js",
  "scripts": {
    "pack-win": "electron-builder --win portable --projectDir prod",
    "pack-win32": "electron-builder --win portable --ia32  --projectDir prod",
    "pack-linux": "electron-builder --linux --projectDir prod",
    "pack-mac": "electron-builder --mac --x64  --projectDir prod ",
    "pack-mac2": "electron-packager ./prod app --platform=darwin --arch=x64 --icon=./icons/app_ico --electron-version=12.0.2 --out pkgs --overwrite"
  },
  "author": "",
  "url": "",
  "repository": {
    "type": "git",
    "url": ""
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "^12.0.4",
    "electron-builder": "^22.10.5"
  }
}
